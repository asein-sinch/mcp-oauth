{
  "name": "sinch-gemini-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server (StreamableHTTP) that verifies the auth server's JWT, resolves the subproject's Sinch credentials from a vault, and calls the Sinch API",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc && rm -rf dist/ui && cp -r ui dist/ui",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.4",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.19.2",
    "google-auth-library": "^10.9.0",
    "jose": "^5.9.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
